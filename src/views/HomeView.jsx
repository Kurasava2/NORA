import React, { useMemo, useRef } from 'react'
import { Badge, Button, Card, Empty, Kpi, PageHead, cx } from '../components/ui.jsx'
import { fmtDate, periodFleetRows, periodMonthName } from '../lib/domain.js'
import { groupPeriods } from '../lib/viewData.js'
export default function HomeView({state,periods,onNew,onOpen,onImport,onDeletePeriod,onDeleteYear}){const inputRef=useRef(null);const summaries=useMemo(()=>periods.map(p=>{const rows=periodFleetRows(state,p);const errors=rows.reduce((n,r)=>n+(r.status.key==='bad'?1:0),0),drove=rows.reduce((n,r)=>n+(r.stats.trips>0?1:0),0);return{id:p.id,rows,errors,drove,stood:(state.vehicles||[]).length-drove}}),[state,periods]);const summaryById=useMemo(()=>new Map(summaries.map(x=>[x.id,x])),[summaries]);const{road,idle,bad}=useMemo(()=>summaries.reduce((a,x)=>{for(const r of x.rows){if(r.status.key==='idle')a.idle++;else a.road++;if(r.status.key==='bad')a.bad++}return a},{road:0,idle:0,bad:0}),[summaries]);const groups=useMemo(()=>groupPeriods(periods),[periods]);return <div className="page"><PageHead title="Расчётные периоды" subtitle={`${(state.vehicles||[]).length} машин · ${state.catalog.length} типов ГСМ · локальная база`} actions={<><input ref={inputRef} className="hidden-input" type="file" accept=".xlsx,.xlsm" onChange={e=>{const f=e.target.files?.[0];e.target.value='';if(f)onImport(f)}}/><Button onClick={()=>inputRef.current?.click()}>⇧ Создать месяц из книги</Button><Button primary onClick={onNew}>＋ Новый период</Button></>}/><div className="kpi-grid"><Kpi value={periods.length} label="месяцев"/><Kpi value={road} label="машино-месяцев с путёвками"/><Kpi value={idle} label="машино-месяцев без выезда" tone="ok"/><Kpi value={bad} label="с ошибками" tone={bad?'bad':undefined}/><Kpi value={(state.vehicles||[]).length} label="машин в справочнике"/></div>{groups.length?<div className="year-sections">{groups.map(([year,items])=><section className="year-section" key={year}><div className="year-heading"><span>{year}</span><small>{items.length} мес.</small><Button small danger onClick={()=>onDeleteYear(year)}>Удалить год</Button></div><div className="card-grid-2">{items.map(p=>{const{errors,drove,stood}=summaryById.get(p.id);return <Card key={p.id} className="period-card"><div className={cx('period-accent',errors&&'bad')}/><div className="row clickable period-card-main" onClick={()=>onOpen(p.id)}><div><h3>{periodMonthName(p)}</h3><p>{fmtDate(p.start)} — {fmtDate(p.end)}</p></div>{errors?<Badge tone="bad">{errors} с ошибками</Badge>:<Badge tone="ok">проверено</Badge>}</div><div className="period-card-stats"><span><b>{drove}</b> ездили</span><span><b>{stood}</b> не ездили</span><div className="period-card-actions"><Button small danger onClick={()=>onDeletePeriod(p.id)}>Удалить</Button></div></div></Card>})}</div></section>)}</div>:<Empty title="Создайте первый расчётный период" text="Периоды будут сгруппированы по годам и месяцам. Можно создать вручную или импортировать готовую Excel-книгу." action={<div className="toolbar"><Button onClick={()=>inputRef.current?.click()}>Импортировать книгу</Button><Button primary onClick={onNew}>Создать период</Button></div>}/>}</div>}
