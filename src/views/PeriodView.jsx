import React, { useDeferredValue, useMemo, useState } from 'react'
import { Badge, Button, Card, Kpi, PageHead } from '../components/ui.jsx'
import useProgressiveRows from '../hooks/useProgressiveRows.js'
import { clone, createStatement, fmtDate, kmfmt, nfmt, periodFleetRows, periodMonthName, periodName, periodYear, vehicleOf } from '../lib/domain.js'
export default function PeriodView({state,period,mutate,onOpen,onExport,onHistory}){const[query,setQuery]=useState(''),[filter,setFilter]=useState('all'),deferredQuery=useDeferredValue(query);const rows=useMemo(()=>periodFleetRows(state,period),[state,period]);const visible=useMemo(()=>rows.filter(r=>{const q=deferredQuery.toLowerCase().trim(),match=!q||`${r.vehicle.shortNo} ${r.vehicle.model} ${r.vehicle.reg}`.toLowerCase().includes(q);return match&&(filter==='all'||r.status.key===filter)}),[rows,deferredQuery,filter]);const counts=useMemo(()=>rows.reduce((o,r)=>{o[r.status.key]=(o[r.status.key]||0)+1;return o},{}),[rows]);const renderedRows=useProgressiveRows(visible,18,18);const openVehicle=vehicle=>{const existing=(period.statements||[]).find(s=>s.vehicleId===vehicle.id);if(existing)return onOpen(existing);const st=createStatement(state,period,vehicleOf(state,vehicle.id));mutate(next=>{const p=next.periods.find(x=>x.id===period.id);if(!p.statements.some(s=>s.vehicleId===vehicle.id))p.statements.push(clone(st))});onOpen(st)};return <div className="page"><PageHead title={periodMonthName(period)} subtitle={`${periodYear(period)} · ${fmtDate(period.start)} — ${fmtDate(period.end)} · контроль автопарка`} actions={<Button onClick={onExport}>Экспорт периода XLSX</Button>}/><div className="kpi-grid"><Kpi value={counts.ok||0} label="готово" tone="ok"/><Kpi value={counts.warn||0} label="с замечаниями"/><Kpi value={counts.bad||0} label="с ошибками" tone={(counts.bad||0)?'bad':undefined}/><Kpi value={counts.idle||0} label="не ездили" tone="ok"/><Kpi value={(state.vehicles||[]).length} label="всего машин"/></div><Card><div className="filters"><input className="input search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Поиск по номеру, модели или рег. номеру…"/><select className="select" value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">Все статусы</option><option value="idle">Не ездили</option><option value="bad">Ошибки</option><option value="warn">Замечания</option><option value="ok">Готово</option></select></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Машина</th><th>Модель</th><th>Рег. №</th><th>Статус</th><th>Путёвок</th><th>Пробег</th><th>Топливо</th><th>Действия</th></tr></thead><tbody>{renderedRows.map(r=><tr key={r.vehicle.id} className="hover-row"><td><b className="mono-lg">{r.vehicle.shortNo}</b></td><td>{r.vehicle.model}</td><td>{r.vehicle.reg}</td><td><Badge tone={r.status.tone}>{r.status.label}</Badge></td><td className="num">{r.stats.trips}</td><td className="num">{kmfmt(r.stats.km)} км</td><td className="num">{nfmt(r.stats.fuel)} л</td><td className="actions-cell"><div className="row-actions"><Button small onClick={()=>onHistory(r.vehicle.id)}>История</Button><Button small primary icon onClick={()=>openVehicle(r.vehicle)} title="Открыть ведомость" aria-label="Открыть ведомость">✎</Button></div></td></tr>)}</tbody></table></div></Card></div>}
