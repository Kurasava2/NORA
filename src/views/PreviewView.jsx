import React, { useEffect, useState } from 'react'
import { Button, Card, PageHead } from '../components/ui.jsx'
export default function PreviewView({vehicle,onBack,onExport,onPrint,getPreviewHtml}){const[html,setHtml]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(true);useEffect(()=>{let cancelled=false;setLoading(true);setError('');getPreviewHtml().then(v=>{if(!cancelled)setHtml(v)}).catch(e=>{if(!cancelled)setError(e?.message||String(e))}).finally(()=>{if(!cancelled)setLoading(false)});return()=>{cancelled=true}},[getPreviewHtml]);return <div className="page page-wide"><PageHead title="Предпросмотр ведомости" subtitle={`${vehicle.model} · ${vehicle.shortNo} · предпросмотр строится из того же Excel-шаблона, что и XLSX`} actions={<><Button onClick={onBack}>← Назад</Button><Button onClick={onExport}>XLSX</Button><Button primary onClick={onPrint}>Печать</Button></>}/>{loading?<Card className="preview-state">Формируется предпросмотр из Excel-шаблона…</Card>:error?<Card className="preview-state preview-error">{error}</Card>:<div className="paper-preview template-preview" dangerouslySetInnerHTML={{__html:html}}/>}</div>}
