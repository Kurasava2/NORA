import React, { useMemo, useState } from 'react'
import { Badge, Button, Card, PageHead } from '../components/ui.jsx'
import { BalancePanel, CarryPanel, CheckPanel, TripTable } from '../components/statement.jsx'
import TripModal from '../modals/TripModal.jsx'
import { buildOpening, fmtDate, nfmt, periodMonthName, periodName, periodYear, reconcileCarryForward, statementMaterialNames, statementStatus, totals, vehicleRateLabel } from '../lib/domain.js'
import { countForm, RU_FORMS } from '../lib/ru.js'
export default function StatementView({state,period,statement,vehicle,mutate,onBack,onHistory,onPreview,onExport,onPrint,notify,confirmAction}){const[editor,setEditor]=useState({open:false,trip:null});const status=useMemo(()=>statementStatus(state,statement,period),[state,statement,period]),total=useMemo(()=>totals(statement),[statement]);const refreshCarry=()=>mutate(next=>{const p=next.periods.find(x=>x.id===period.id),st=p.statements.find(x=>x.id===statement.id);st.opening=buildOpening(next,st.vehicleId,p);reconcileCarryForward(next,st)});const deleteTripFromList=async trip=>{if(!await confirmAction({title:'Удалить путёвку?',message:`Путёвка №${trip.number||'—'} от ${fmtDate(trip.date)} будет удалена.`,confirmText:'Удалить',danger:true}))return;mutate(next=>{const p=next.periods.find(x=>x.id===period.id),st=p.statements.find(x=>x.id===statement.id);st.trips=st.trips.filter(t=>t.id!==trip.id);reconcileCarryForward(next,st)});notify('Путёвка удалена')};return <div className="page page-wide"><PageHead title={`${vehicle.model} · ${vehicle.shortNo}`} subtitle={`№ ${vehicle.reg} · ${periodMonthName(period)} ${periodYear(period)} · ${periodName(period)}`} actions={<><Button onClick={onBack}>← К месяцу</Button><Button onClick={onHistory}>История</Button><Button onClick={onPreview}>Предпросмотр</Button><Button onClick={onExport}>XLSX</Button><Button primary onClick={onPrint}>Печать</Button></>}/><div className="statement-strip"><div><span>Статус</span><Badge tone={status.tone}>{status.label}</Badge></div><div><span>Норма</span><b>{vehicleRateLabel(vehicle)}</b></div><div><span>Бак</span><b>{vehicle.tankCapacity?`${nfmt(vehicle.tankCapacity)} л`:'—'}</b></div><div className="grow"><span>ГСМ в путёвках</span><div className="chip-row">{statementMaterialNames(statement).map(m=><span className="chip" key={m}>{m}</span>)}</div></div></div><div className="workspace"><div className="workspace-main"><Card><div className="section-heading"><div><h3>Внесённые путёвки</h3><p>Нажмите строку, чтобы отредактировать. Новая путёвка открывается отдельным окном.</p></div><div className="section-actions"><Badge tone="blue">{countForm(statement.trips.length,RU_FORMS.trip)}</Badge><Button primary onClick={()=>setEditor({open:true,trip:null})}>＋ Добавить путёвку</Button></div></div><TripTable state={state} period={period} statement={statement} vehicle={vehicle} onEdit={trip=>setEditor({open:true,trip})} onDelete={deleteTripFromList}/></Card><BalancePanel statement={statement} total={total}/></div><aside className="workspace-side"><CheckPanel status={status}/><CarryPanel statement={statement} onRefresh={refreshCarry}/></aside></div><TripModal open={editor.open} trip={editor.trip} onClose={()=>setEditor({open:false,trip:null})} state={state} period={period} statement={statement} vehicle={vehicle} mutate={mutate} notify={notify} confirmAction={confirmAction}/></div>}
