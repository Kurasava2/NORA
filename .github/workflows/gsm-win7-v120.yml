name: Legacy GSM build (manual only)

on:
  workflow_dispatch:

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow retained for history; v1.3.0 uses gsm-win7-v130.yml"
