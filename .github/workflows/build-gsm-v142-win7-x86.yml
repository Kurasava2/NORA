name: GSM v1.4.2 staging maintenance

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  idle:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Staging exact v1.4.2 overlay; automatic build temporarily paused."
